syntax = "proto2";

import "OpenApiCommonMessages.proto";

/** Proxy bi-directionnel d'enveloppes ProtoMessage */
service OpenApiService {
  rpc Process (stream ProtoMessage) returns (stream ProtoMessage);
}